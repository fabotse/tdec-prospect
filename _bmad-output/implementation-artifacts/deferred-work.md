# Deferred Work

Itens reais identificados em reviews mas adiados (pré-existentes ou fora do escopo da story revisada).

## Deferred from: code review of story-21.7 (2026-07-16)

> Review adversarial 3 camadas (Blind Hunter / Edge Case Hunter / Acceptance Auditor), modo full. **Acceptance Auditor: 7/7 ACs SATISFEITOS + 12 anti-patterns respeitados.** Nenhum bug HIGH. Convergência forte: o CAS de `notified_at` ordenado após os side-effects (achado nº1) foi eleito de forma INDEPENDENTE por Blind + Edge — virou patch na story. 1 decision-needed + 4 patches tratados na story; os 5 defers abaixo são pré-existentes / de schema / operacionais / de escala.

- **Falha na in-app INSERT ainda marca `notified_at` → registro perdido para sempre (fail-open assimétrico).** [notification-processor.ts:275-323](../../src/lib/utils/notification-processor.ts#L275-L323): uma falha transitória (deadlock/timeout) no INSERT de `app_notifications` é logada mas NÃO impede o `.update({notified_at})` — a opp nunca reentra no passe (`.is("notified_at", null)`), então o registro que o AC2 promete "para qualquer oportunidade nova" nunca é criado e nunca é recuperado. O fail-open da Task 3.6 raciocina só sobre o WhatsApp falhar, nunca sobre o canal durável (in-app) falhar. Nenhum teste exercita erro de INSERT in-app. Fix futuro: retry de in-app ou não marcar `notified_at` quando a in-app estava habilitada e falhou. Severidade: MEDIUM (transitório, raro). Fonte: Acceptance Auditor, review 21.7.
- **Central de notificações é tenant-shared (sem `user_id`); mark-read é global.** [app_notifications 00055](../../supabase/migrations/00055_create_opportunities_schema.sql) + [notifications/[notificationId]/route.ts:27-33](../../src/app/api/notifications/[notificationId]/route.ts#L27-L33): `app_notifications` só tem `tenant_id` (sem `user_id`); GET/unread-count/PATCH filtram só por tenant. Um SDR que abre o sino e clica numa notificação zera `read_at` (e o badge de não-lidas) para o gestor e todos os SDRs do tenant. Se o sino era para ser por-usuário, está errado; se é caixa compartilhada, ok. Limitação de schema PRÉ-EXISTENTE da 21.1 — corrigir exige `user_id` + migration, fora do escopo da 21.7. **Confirmar intenção de produto.** Severidade: LOW. Fonte: Blind Hunter, review 21.7.
- **Ordering de deploy: o cron pode criar in-app do backlog antes do suppress-backfill rodar.** [notification-processor.ts:472-479](../../src/lib/utils/notification-processor.ts#L472-L479): a 00060 adiciona `notified_at` NULL a toda opp existente; nada no código força que o backfill (`suppressOnly`) preceda o 1º tick do cron pós-migration. O freshness-guard limita o WhatsApp (histórico é velho), mas NÃO há guard de idade no INSERT in-app → flood de in-app do backlog. Baixo impacto hoje (cliente ~4 opps). Mitigação operacional: rodar o suppress-backfill imediatamente após aplicar a 00060 (adicionar ao `epic-21-post-deploy-checklist.md`). Severidade: LOW. Fonte: Blind Hunter + Edge Case Hunter, review 21.7.
- **`MAX_NOTIFY_PER_RUN = 200` é global, não por-tenant.** [notification-processor.ts:41,472-479](../../src/lib/utils/notification-processor.ts#L41): o cron (sem `tenantId`) capa 200 linhas TOTAL, ordenadas `created_at ASC` cruzando todos os tenants → o backlog de um tenant barulhento pode faminto os demais, e combinado com a janela de 60 min um tenant faminto pode ver leads quentes genuínos envelhecerem além da elegibilidade do WhatsApp. Irrelevante no deploy single-tenant atual; o cap está no lugar errado. Fix futuro: cap por-tenant. Severidade: LOW. Fonte: Blind Hunter, review 21.7.
- **`notificationId` não-UUID devolve 500 em vez de 404/400.** [notifications/[notificationId]/route.ts:27-40](../../src/app/api/notifications/[notificationId]/route.ts#L27-L40): `.eq("id", "garbage")` levanta `22P02 invalid input syntax for type uuid` → ramo genérico `INTERNAL_ERROR` → 500, mascarando o not-found. **MESMO padrão já deferido na review 21.5** (rotas de opportunities/suggestion) — decisão consistente do codebase (o path param só chega via UI com UUID real). Fix futuro: helper de validação de UUID ou mapear `22P02` → 404. Severidade: LOW. Fonte: Edge Case Hunter, review 21.7.

## Deferred from: code review of story-21.2 (2026-07-13)

> Review adversarial 3 camadas (Blind Hunter / Edge Case Hunter / Acceptance Auditor), modo full. **9/9 ACs SATISFEITOS** (AC9 parcial — cobertura de rate-limit/cap indireta), 10/10 anti-patterns respeitados. 6 patches + 1 decision-needed tratados na story. O defer abaixo é pré-existente (não introduzido pela 21.2 — ela apenas espelhou o padrão como as Dev Notes mandavam).

- **Fail-open de auth quando o cron secret está ausente (padrão sistêmico das rotas de cron).** [monitoring/process-batch/route.ts:35-37](../../src/app/api/monitoring/process-batch/route.ts#L35-L37): `authHeader !== \`Bearer ${monitoringSecret}\`` sem checar `!monitoringSecret` → se o env var estiver ausente/vazio, `Authorization: Bearer undefined` passa e dispara a rota service-role. A 21.2 corrige isso na própria rota nova (`replies/process-batch`), mas o mesmo gap existe na rota de monitoring (pré-existente, story 13.3) e provavelmente em qualquer outra rota de cron que siga o mesmo template. Mitigado hoje: o secret está provisionado em prod. Fix: hardening cross-cutting (`!secret ||` no guard de todas as rotas de cron), idealmente um helper compartilhado `requireCronSecret(req, envName)`. Severidade: MEDIUM (fail-open latente em fronteira de auth; não explorável enquanto o secret estiver setado). Fonte: Blind Hunter + Edge Case Hunter, review 21.2.

## Deferred from: code review of story-21.1 (2026-07-13)

> Review adversarial 3 camadas (Blind Hunter / Edge Case Hunter / Acceptance Auditor), modo full. **0 patch, 0 decision-needed.** Acceptance Auditor: **6/6 ACs SATISFEITOS**, 0 violação de AC, 0 anti-pattern quebrado → ACCEPT. 27/27 testes da story + eslint reconfirmados de forma independente. Nenhum defeito no escopo entregue (fundação = schema + tipos + testes). **Fato de roteamento: a migration `00055` já foi aplicada no banco do cliente** (gerido à mão, `CREATE TABLE IF NOT EXISTS`) → qualquer hardening de schema exige migration futura e pertence às stories consumidoras. Os defers abaixo são riscos/hardening downstream, não regressões da 21.1.

- **Premissa de idempotência de reply não forçada no schema (NFR2).** [00055_create_opportunities_schema.sql:30-47](../../supabase/migrations/00055_create_opportunities_schema.sql#L30-L47): `source='reply'` aceita `reply_event_id` NULL; no Postgres múltiplos NULL são distintos no índice UNIQUE, então dois ingests do mesmo reply sem `reply_event_id` resolvido duplicam o card. A idempotência (`UNIQUE(reply_event_id)`) só vale se **a 21.2 sempre popular `reply_event_id`** e usar `ON CONFLICT DO NOTHING`. **Dono: Story 21.2.** Opção belt-and-suspenders (migration futura): `CHECK (source <> 'reply' OR reply_event_id IS NOT NULL)`. Severidade: MEDIUM (forward-risk, sem consumidor hoje). Fonte: Blind Hunter + Edge Case Hunter, review 21.1.
- **Engajamentos sem chave de dedup.** [00055_create_opportunities_schema.sql:44-46](../../supabase/migrations/00055_create_opportunities_schema.sql#L44-L46): `source='engagement'` tem `reply_event_id` NULL por design → nenhum constraint impede duplicatas quando o polling reprocessa a mesma abertura/clique. **Dono: Story 21.2/21.6** — dedup na app ou índice parcial `UNIQUE(tenant_id, campaign_id, lead_id) WHERE source='engagement'` em migration futura. Severidade: MEDIUM (forward-risk). Fonte: Blind Hunter, review 21.1.
- **`toNotificationSettings` não defende `channels` JSONB parcial/malformado.** [opportunity.ts:184-187](../../src/types/opportunity.ts#L184-L187): chave `in_app` ausente → `inApp: undefined` enquanto o tipo declara `boolean` (um `if (channels.inApp)` downstream trata como desabilitado silenciosamente); `channels` = null/array/scalar → `TypeError`. Segue a **convenção do projeto** (transforms fazem mapeamento direto sem defesa — ver `transformOpportunityConfigRow`/`transformMonitoringConfigRow`; confiam no schema+default). Revisitar quando **21.7** criar o writer de settings (default `?? false` no transform ou CHECK de shape no JSONB). Severidade: LOW. Fonte: Blind Hunter + Edge Case Hunter, review 21.1.
- **Conteúdo de JSONB sem validação nos transforms.** [opportunity.ts:183,188,224](../../src/types/opportunity.ts#L183): `notify_intents` (tipado `OpportunityIntent[]`) pode conter intent inválida sem passar por `isValidOpportunityIntent`; `whatsapp_numbers` sem checagem de elemento; `payload` não-objeto (array/scalar/JSON-null) mistipado como `Record<string, unknown>`. Sem consumidor hoje. **Dono: Story 21.7** (validar na escrita ou CHECK de conteúdo). Severidade: LOW. Fonte: Edge Case Hunter, review 21.1.
- **`app_notifications` "imutável exceto read_at" documentado mas não enforçado.** [00055_create_opportunities_schema.sql:178-182](../../supabase/migrations/00055_create_opportunities_schema.sql#L178-L182): o comentário (linha 157) promete imutabilidade exceto `read_at`, mas a policy de UPDATE só valida `tenant_id` — um usuário do próprio tenant pode reescrever `type`/`payload`. **Sem brecha cross-tenant**; writes reais são via service-role. Enforçar via trigger ou policy coluna-scoped quando o update path (marcar-como-lida) for construído. **Dono: Story 21.7.** Severidade: LOW. Fonte: Blind Hunter + Edge Case Hunter, review 21.1.
- **Sem CHECK de coerência de lifecycle do card.** [00055_create_opportunities_schema.sql:39-41](../../supabase/migrations/00055_create_opportunities_schema.sql#L39-L41): nada acopla `meeting_booked_at` a `status='meeting_booked'`, e o CHECK valida o valor de `status` mas não a transição (`new → viewed → contacted → meeting_booked | discarded` é ilustrativo). **Dono: Story 21.5** (ações do card). Severidade: LOW. Fonte: Edge Case Hunter, review 21.1.

## Deferred from: quick-dev "deletar segmento no filtro" (2026-07-02)

> Review adversarial 3 camadas (Blind / Edge-case / Acceptance). Acceptance auditor: 100% compatível com os 5 ACs e todas as constraints. Patches reais aplicados (reset de filtro órfão, `preventDefault` anti-duplo-submit + `isPending` visível). Defer abaixo é real mas segue o padrão já existente no codebase.

- **A11y: `<button>` de lixeira aninhado em `role="menuitem"` + delete inacessível por teclado.** Em [SegmentFilter.tsx](../../src/components/leads/SegmentFilter.tsx) cada item de segmento é um `DropdownMenuItem` (role menuitem) que agora contém um `<DeleteSegmentButton>` (um `<button>`). Aninhar controle interativo dentro de menuitem é violação ARIA, e a navegação por setas do Radix foca a *linha*, não o botão interno → o delete é inalcançável por teclado (Enter na linha filtra). **Pré-existente**: é exatamente o mesmo padrão de [SegmentDropdown.tsx](../../src/components/leads/SegmentDropdown.tsx) (que já tinha a lixeira no item). Fix futuro: reestruturar a linha para separar o alvo de seleção do botão de ação (ou mover o delete para uma tela/menu de gestão dedicado). Severidade: LOW (a11y; padrão consistente com o resto do app). Fonte: Blind Hunter #3 + Edge Case Hunter #9.

## Deferred from: code review of story-20.5 (2026-06-16)

> Review adversarial 3 camadas (Blind Hunter / Edge Case Hunter / Acceptance Auditor), modo full. **0 bug HIGH/MEDIUM de código.** O Acceptance Auditor confirmou os **7 ACs SATISFEITOS** + anti-patterns + Task 7. Findings de código mais fortes (fail-open em `isOnlyAdmin`/middleware) **refutados** por defesa em profundidade (guard inline fail-closed na `removeTeamMember`; `hasAdminAccess(undefined)→false` no middleware). 2 patches de test-hardening ficaram como itens da story (não-bloqueantes). Defers abaixo são reais mas pré-existentes / arquiteturais / latentes — não introduzidos pela 20.5.

- **`refetchProfile` sem timeout/fallback (UI, infra `useUser`).** O load inicial do `useUser` tem fallback de ~5s ([use-user.ts:199-212](../../src/hooks/use-user.ts#L199-L212)), mas `refetchProfile` não — ele seta `isProfileLoading=true`, aguarda `fetchProfileData`, e só então volta a `false`. Se a query `single()` travar (stall de rede) durante o refetch do auto-rebaixamento (disparado pelo `useTeamMembers`), `isProfileLoading` fica `true` indefinidamente → `canSeeAdmin` vira false → o Sidebar esconde Configurações/Technographic de um **Gestor/Diretor legítimo** até reload. `fetchProfileData` captura seus próprios erros (volta `null` e reseta o flag), então só um *hang* real (não erro) dispara isso. **Sem brecha de segurança** (esconde demais, não de menos). Severidade: LOW (UX, baixa probabilidade). Fonte: Edge Case Hunter, review 20.5.
- **Middleware não gateia rotas protegidas não-admin por perfil → usuário autenticado sem linha em `profiles` alcança `/leads`,`/campaigns`.** O check de papel ([middleware.ts:84-95](../../src/lib/supabase/middleware.ts#L84-L95)) só roda dentro do bloco `isAdminRoute`. Para `/leads`/`/campaigns` (protegidas, não-admin) o middleware só verifica sessão, não perfil. Um usuário meio-provisionado (auth existe, perfil ausente — exatamente a corrida convite→signup que o codebase trata em outro lugar) passa direto, dependendo só da RLS como backstop de dados. **Pré-existente** (o middleware nunca consultou perfil fora de rotas admin; a 20.5 só somou `/technographic`). Rotas admin **são** fail-closed (`hasAdminAccess(undefined)→false→/leads`). O novo `middleware.test.ts` também não exercita o caso "autenticado, perfil ausente". Severidade: MEDIUM (residual; RLS mitiga). Fonte: Edge Case Hunter, review 20.5.
- **`startsWith("/technographic")` sobre-casa irmãos + futuro `/api/technographic` receberia 302 em vez de 403.** O matcher ([middleware.ts:52,61](../../src/lib/supabase/middleware.ts#L52)) casa `/technographic`, `/technographic/*` E `/technographic-foo`/`/technographicz`. Hoje não há rota irmã (latente; segue a mesma convenção bare-`startsWith` do `/settings`). Ponto real para o futuro: as rotas de dados technographic (`/api/integrations/theirstack/*`) **não** estão sob `/technographic`, então o gate do middleware não as cobre (elas se auto-protegem com 403 in-handler). Se um dia for criado `/api/technographic/*`, o middleware o trataria como admin-route e devolveria **redirect 302 → /leads** em vez de **403 JSON**, quebrando o contrato de API (inconsistente com as rotas theirStack). Fix futuro: casar `=== "/technographic" || startsWith("/technographic/")` e/ou excluir `/api/*` do gate de redirect. Severidade: LOW (latente). Fonte: Edge Case Hunter, review 20.5.
- **`/insights` e `/agent` fora de `isProtectedRoute` (sub-gate de sessão no middleware).** Ambas as superfícies autenticadas ([middleware.ts:48-52](../../src/lib/supabase/middleware.ts#L48-L52)) não estão na lista de rotas protegidas → uma navegação direta **não autenticada** para `/insights` ou `/agent` não é redirecionada para `/login` pelo middleware (a página/RLS precisa barrar). Ortogonal ao eixo SDR-vs-admin (não é vazamento de SDR), mas é um under-match de rota-protegida na superfície que esta story de auditoria toca. **Pré-existente.** Severidade: LOW. Fonte: Edge Case Hunter, review 20.5.

## Deferred from: code review of story-20.4 (2026-06-16)

> Review adversarial 3 camadas (Blind Hunter / Edge Case Hunter / Acceptance Auditor). 0 bug HIGH confirmado na ação de auth (`applyInvitedRoleOnAcceptance`) — o Acceptance Auditor (com contexto) confirmou conformidade total com AD-5 e os 4 ACs. 1 patch real fora dos defers (normalização de e-mail no `inviteUser`, falha segura). Defers abaixo são reais mas não acionáveis agora (verificação operacional ou robustez de ops tooling).

- **AC#4 sem cobertura automatizada do transporte cookie→server action.** O wiring no callback ([page.tsx:72](../../src/app/auth/callback/page.tsx#L72)) está testado (ordem `setSession < action < signOut` pinada), mas o teste mocka a ação — o transporte real do cookie de sessão (browser `setSession` → `getUser()` server-side na server action) NÃO é coberto por teste automatizado. Se o cookie não propagar a tempo, a ação no-opa (`applied:false`) e o convidado fica como `sdr` em silêncio (falha segura). A própria spec defere isso à verificação manual (§Execução item 7: convidar um e-mail teste como `gestor` → aceitar → confirmar `profiles.role = gestor`). **Verificar manualmente na entrega.** Severidade: MEDIUM (residual real; cobertura zero do caminho security-critical). Fonte: Blind Hunter + Acceptance Auditor.
  - **Atualização (teste ao vivo 2026-06-16):** investigação do "link inválido/expirado" (sintoma reportado no `reset-user-password.mjs`). Via `admin.generateLink({type:'invite'})` + seguindo o `/verify` sem redirect, o convite volta **IMPLÍCITO** (`http://.../auth/callback#access_token=...&refresh_token=...&type=invite`, status 303) — **compatível com o `/auth/callback` atual**. Hipótese de incompatibilidade PKCE **refutada para o convite** (o `?code=` PKCE só aparece em fluxos iniciados no cliente, como o reset; convite de admin passa pelo `/verify` server-side e devolve sessão no hash). O "quebrado" do reset é fluxo diferente (provável causa ambiental: cross-device/prefetch), fora do escopo da 20.4.
  - **Teste END-TO-END completo (2026-06-16):** convite descartável `gestor` criado na base do cliente → link aberto num **browser real (Playwright)** no app local → `profiles.role` foi de `sdr` para **`gestor`** e `team_invitations.status` virou **`accepted`** (`accepted_at` preenchido), **0 erro no console**. Throwaway (usuário + convite) limpo depois. **Conclusão: o hop cookie→server FUNCIONA** — a server action enxerga a sessão recém-gravada pelo `setSession`. O risco do defer #1 está **resolvido empiricamente**; a lacuna remanescente é só de cobertura **automatizada** na suíte (test-hardening opcional futuro), não de comportamento. **Severidade rebaixada para LOW.** Único item operacional real que sobra para convites em produção = o `NEXT_PUBLIC_SITE_URL` abaixo.
- **`NEXT_PUBLIC_SITE_URL` não setado → `redirectTo` do convite cai em `localhost:3000` (release/config).** [team.ts:201](../../src/actions/team.ts#L201) faz `siteUrl = process.env.NEXT_PUBLIC_SITE_URL || "http://localhost:3000"`; o `.env.local` não tem `NEXT_PUBLIC_SITE_URL`. Para convites via UI funcionarem no ambiente publicado, setar `NEXT_PUBLIC_SITE_URL` para a URL de produção **e** garantir essa URL de callback na allowlist de Redirect URLs do projeto Supabase. **Não afeta a entrega dos 4 usuários** (script não usa e-mail/redirect). Severidade: MEDIUM (config de deploy; bloqueia convite via UI em prod). Fonte: investigação ao vivo da review 20.4.
- **Convidado aparece 2x na lista de time + status "Ativo" falso (pré-existente, Story 2.7).** `getTeamMembers` ([team.ts:88-114](../../src/actions/team.ts#L88-L114)) junta `profiles` (TODOS com `status: "active"` hardcoded — [team.ts:96](../../src/actions/team.ts#L96)) com `team_invitations` pending. Como `inviteUserByEmail` cria o auth user na hora e o trigger `handle_new_user` cria o perfil imediatamente, todo convidado aparece como **membro "Ativo"** (perfil, papel `sdr` default) **E** como **"Pendente"** (convite, papel convidado) até aceitar — e o "Ativo" é mentira (não aceitou/sem senha). Some após a aceitação (feature B marca o convite `accepted`), mas é confuso antes; com papel ≠ `sdr` fica pior (linha "Ativo" mostra `sdr`, "Pendente" mostra o papel real). Observado no teste manual de convite (2026-06-16, localhost). Fix sugerido: dedup por e-mail + derivar o status real do auth user (`email_confirmed_at`/`last_sign_in_at`) em vez de hardcodar "active". **Não afeta o script de provisionamento** (não cria `team_invitations` → 1 linha só). Severidade: MEDIUM (UX, pré-existente). Dono sugerido: polish da Story 2.7 ou 20.5.
- **Supabase não envia o e-mail de convite (operacional).** No teste manual (2026-06-16) o `inviteUser`/`inviteUserByEmail` não entregou e-mail — provável rate-limit do e-mail nativo do Supabase (~3-4/h, só para teste) ou SMTP custom não configurado no projeto. Sem isso o convite-pela-UI não fecha o ciclo (convidado não recebe o link → não aceita → feature B não roda em uso real). **Não afeta a entrega** (script não usa e-mail). Fix: configurar SMTP custom (Resend/SendGrid/SES) no projeto Supabase. Severidade: MEDIUM (operacional; bloqueia convite-pela-UI). Fonte: teste manual da review 20.4.
- **Robustez de idempotência do script de provisionamento (ops tooling).** Três gaps no `provision-client-users.mjs`, todos latentes na entrega atual (DB pequeno verificado, só `seste` pré-existe): (a) `listUsers({page:1, perPage:1000})` ([linha 98-101](../../scripts/provision-client-users.mjs#L98-L101)) não pagina → com >1000 users um usuário existente vira "novo" → `createUser` "already registered" → papel NÃO aplicado; (b) o erro "already registered" ([linha 134-142](../../scripts/provision-client-users.mjs#L134-L142)) faz `continue` e pula o update de papel (contradiz a idempotência declarada); (c) timing do trigger `handle_new_user` (AFTER INSERT) pode fazer o update do perfil rodar antes da linha existir → guarda de 0 linhas dispara como erro para um usuário que FOI criado (re-run recupera). Fix futuro: paginar `listUsers`; no "already registered" buscar o user e aplicar o papel; retry/poll no update do perfil recém-criado. Severidade: LOW (ops tooling, DB pequeno, re-executável). Fonte: Edge Case Hunter + Blind Hunter.
- **Script realoca usuário existente entre tenants sem aviso.** O ramo "já existe" ([linha 148-152](../../scripts/provision-client-users.mjs#L148-L152)) grava `tenant_id: tenant.id` por `id` sem ler/comparar o tenant atual nem avisar no resumo. Para a entrega é benigno (`seste` já está no tenant alvo `...0001`); mas se um dos 4 e-mails existisse em outro tenant, seria movido em silêncio. Fix futuro: read-back do `tenant_id` atual + aviso "movido X→Y" no resumo (ou confirmação explícita). Severidade: LOW (latente). Fonte: Edge Case Hunter + Acceptance Auditor.
- **`full_name` dos usuários do script são placeholders derivados do e-mail.** `CLIENT_USERS` ([linha 43-48](../../scripts/provision-client-users.mjs#L43-L48)) grava "M. Fabossi"/"C. Case"/"R. Gomes" (inicial + sobrenome chutado do prefixo do e-mail) no `user_metadata` dos usuários NOVOS — pode estar errado (ex.: "C. Case" vs sobrenome real "Casey") e fica abreviado. **Decisão Fabossi 2026-06-16: manter por enquanto** — nomes reais dos 3 novos ainda desconhecidos; ajustar o `CLIENT_USERS` na hora de rodar a OP quando os nomes forem conhecidos. Só afeta usuários novos (existentes mantêm o nome). Severidade: LOW (cosmético). Fonte: Acceptance Auditor.

## Deferred from: code review of story-20.3 (2026-06-16)

> Review adversarial 3 camadas (Blind Hunter / Edge Case Hunter / Acceptance Auditor), modo YOLO. 0 bug HIGH. ACs 1/2/3 verificados independentemente pelo Acceptance Auditor (sem scope creep p/ 20.4/20.5). 2 patches aplicados nesta review (test-hardening dos predicados de segurança em `updateMemberRole` + `console.error` no caminho de 0 linhas). Defers abaixo são reais mas pré-existentes/arquiteturais — roteados para a **20.5** (auditoria defense-in-depth).

- **Invariante "≥1 admin" vive só na camada de aplicação (TOCTOU + fail-open + sem garantia no banco).** A proteção do último admin em `updateMemberRole` ([team.ts:441-459](../../src/actions/team.ts#L441-L459)) é um check-then-update **não transacional**: (a) **TOCTOU** — dois admins rebaixados concorrentemente passam ambos pelo `count===2` e o tenant fica com 0 admins; (b) **fail-open** — se a query de contagem falhar e retornar `count: null`, `null === 1` é `false` → o rebaixamento prossegue; (c) a RLS (00054) só garante "o ator é admin", **não** "resta ≥1 admin". Mesmo padrão já existe em `removeTeamMember`/`isOnlyAdmin` (pré-existente, não regressão da 20.3) e a spec mandou espelhá-lo. Fix robusto = trigger/constraint no banco ou contagem em transação serializável. Severidade: MEDIUM (concorrência/edge; baixíssima probabilidade com ~4 usuários). **Dono: Story 20.5.** Fonte: Blind Hunter + Acceptance Auditor, review 20.3.
- **Auto-rebaixamento deixa o estado de admin do cliente obsoleto (`useUser`).** Quando há ≥2 admins, a UI habilita "Alterar Função" na **própria** linha do usuário ([TeamMemberList.tsx:95-96](../../src/components/settings/TeamMemberList.tsx#L95-L96)) e o servidor permite o auto-rebaixamento (count=2). Mas a mutation de papel só invalida `["team","members"]` e `["team","isOnlyAdmin"]` ([use-team-members.ts:108-114](../../src/hooks/use-team-members.ts#L108-L114)); o `useUser` mantém `role`/`isAdmin` num singleton de módulo **fora** do TanStack Query, então `AdminGuard`/sidebar continuam mostrando UI de admin até reload/re-login. **Sem brecha de segurança** (o servidor barra ações privilegiadas), só estado de UI falso. Severidade: MEDIUM (UX, raro). **Dono: Story 20.5** (varredura SDR/superfícies admin é o lugar natural). Fonte: Edge Case Hunter, review 20.3.
- **Policy RLS de UPDATE é row-level (não restringe a coluna `role`).** A 00054 concede UPDATE de **qualquer coluna** de perfis do mesmo tenant ao admin — RLS no Postgres não restringe colunas. Mitigado: a única caller (`updateMemberRole`) escreve apenas `{ role }`, e a policy é **simétrica** à de DELETE de admin já existente (00009). Endurecimento opcional (trigger que limite as colunas alteráveis) cabe na auditoria. Severidade: LOW. **Dono: Story 20.5** (nota de hardening). Fonte: Blind Hunter, review 20.3.

## Deferred from: code review of story-20.1 (2026-06-16)

- **Gap convite→signup: papel convidado não é aplicado.** `handle_new_user()` ([00053:144](../../supabase/migrations/00053_expand_roles_to_gestor_diretor_sdr.sql)) insere sempre `role = 'sdr'` e **não** lê `raw_user_meta_data->>'role'`, embora `inviteUser` grave o papel escolhido em `user_metadata` e em `team_invitations.role` ([team.ts:205/228](../../src/actions/team.ts)). Logo, um admin que convida alguém como `gestor`/`diretor` resulta em perfil criado como `sdr`. **Decisão consciente de segurança** (honrar metadata é vetor de escalonamento via `/signup`); correção segura = lookup em `team_invitations` na pós-aceitação. **Dono: Story 20.4.** Severidade: MEDIUM. Fonte: Edge Case Hunter, review 20.1.
- **Ordem de deploy vs migration (operacional).** O código novo rejeita os literais antigos `'admin'`/`'user'` (`isValidRole`/`inviteUserSchema`) e `hasAdminAccess('admin')→false`. Se o deploy do código preceder a aplicação da migration 00053, perfis ainda em `'admin'`/`'user'` quebram (admins migrados perdem acesso → NFR-C1, `ROLE_LABELS[role]→undefined`). **Mitigação: aplicar a migration 00053 antes/junto do deploy.** Sem fix de código — é checklist de release. Severidade: MEDIUM (operacional). Fonte: Blind Hunter, review 20.1.
  - **Update (20.3):** soma-se a **migration 00054** (policy `"Admins can update tenant profiles"` em `profiles`, via `is_admin()`+tenant) — necessária para a edição de papel gravar. Sem ela a action retorna erro honesto (não falso sucesso), mas a gravação só funciona após aplicar. Aplicar 00054 junto da 00053 no banco gerido à mão (Supabase Dashboard → SQL Editor).

## Deferred from: code review of story-20.2 (2026-06-16)

> Itens HIGH (deploy×migration e convite→signup) **já estão rastreados** na seção da review 20.1 acima — não repetidos aqui. Novos itens desta review:

- **Test-hardening papel×capacidade (patch P1 deferido).** [team.test.ts](../../__tests__/unit/actions/team.test.ts) / [use-user.test.ts](../../__tests__/unit/hooks/use-user.test.ts): (a) asserir que a contagem de "último admin" chama `.in("role", ["gestor","diretor"])` (mock trocou `.eq`→`.in` sem verificar argumento); (b) adicionar caso `role: "diretor"` em ao menos um guard de servidor (hoje só gestor/sdr são exercitados — regressão p/ `=== "gestor"` não seria pega); (c) asserir `isAdmin === false` quando `profile` é null no caminho `hasAdminAccess(state.profile?.role)`. Não bloqueante (suíte verde; `capabilities.test.ts` cobre papel×capacidade isolado). Decisão Fabossi 2026-06-16: deferido. Severidade: LOW. Fonte: Blind Hunter + Acceptance Auditor, review 20.2.
- **Fallback de badge de papel (patch P2 deferido).** ~~[TeamMemberList.tsx:113](../../src/components/settings/TeamMemberList.tsx#L113) renderiza `ROLE_LABELS[member.role]` sem fallback → badge vazio para papel fora do enum.~~ ✅ **RESOLVIDO na Story 20.3** (`ROLE_LABELS[member.role] ?? member.role` + teste de fallback). Severidade: LOW. Fonte: Blind Hunter + Edge Case Hunter, review 20.2.
- **AC3 overclaim — SDR vê o link "Configurações" no Sidebar (vazamento de UI).** [Sidebar.tsx:54](../../src/components/common/Sidebar.tsx#L54) renderiza `/settings` incondicionalmente, sem gate `isAdmin`. O servidor barra via redirect ([middleware.ts:76](../../src/lib/supabase/middleware.ts#L76)) → sem brecha de segurança real, mas o AC3 da 20.2 afirma "negado em todas as camadas". **Dono: Story 20.5** (varredura anti-vazamento SDR — UI + servidor). Decisão Fabossi 2026-06-16: deferido (vazamento é só de UI; servidor já barra; gate de UI é escopo explícito da 20.5). Severidade: MEDIUM. Fonte: Acceptance Auditor, review 20.2.
  - **UX observada (Fabossi, 2026-06-16, teste visual 20.3):** logado como SDR, clicar em "Configurações" **redireciona em silêncio** para `/leads` — sem aviso. O `AdminGuard` fallback nunca aparece porque o middleware intercepta antes. **Fix preferido na 20.5: esconder o link no Sidebar para SDR** (gate `hasAdminAccess`/`isAdmin`) — não mostrar o que não pode usar. Mensagem/toast no redirect = opcional.
- **`isAdminRole` é dead code de produção.** [src/types/database.ts:233](../../src/types/database.ts#L233) delega a `hasAdminAccess` mas não tem nenhum call-site em `src/` (só a própria definição + `database.test.ts`). Além de inútil, importa um helper de runtime (`hasAdminAccess`) para dentro de um módulo de **tipos**, criando um ciclo de import type-level. Limpeza sugerida: remover `isAdminRole` (+ teste) ou, se for API pública intencional, documentar. Severidade: LOW. Fonte: Blind Hunter, review 20.2.
- **InviteUserDialog não restringe quais papéis um admin pode atribuir.** [InviteUserDialog.tsx](../../src/components/settings/InviteUserDialog.tsx) oferece os 3 papéis (incl. gestor/diretor) sem gate de capacidade do convidador. ✅ **RESOLVIDO por decisão na Story 20.3:** sem gate por enquanto — gestor == diretor e não há superfície "só gestor"/"só diretor" (matriz §4); qualquer admin pode atribuir qualquer papel. Única regra protetiva = "último admin". Severidade: LOW. Fonte: Blind Hunter, review 20.2.
- **Proteção "último admin" ignora convites pendentes.** A contagem em `removeTeamMember`/`isOnlyAdmin` ([team.ts:310/404](../../src/actions/team.ts#L310)) usa só `profiles`; convites pendentes em `team_invitations` com papel admin não entram na conta. Pré-existente (não regressão da 20.2). Severidade: LOW. Fonte: Blind Hunter, review 20.2.
- **161 erros de `tsc` em `__tests__/` (0 em `src/`).** O código de produto é type-clean, mas a suíte de testes acumula 161 erros de tipo (snovio.test, monitoring-processor.test, agent.test, campaign.test, migrations/00042) que passam despercebidos por não haver script `typecheck`/`tsc` no `package.json` (vitest não faz typecheck). Dívida pré-existente, fora do escopo do Epic 20. Severidade: LOW. Fonte: Acceptance Auditor, review 20.2.
- **Higiene de processo: código do Epic 20 não commitado.** A 20.2 reivindica "nenhum código novo" e sugere pular re-review, porém `capabilities.ts` + todos os swaps estão **untracked/working-tree** sobre o baseline da Epic 19 (`76895d6`) — ou seja, o código que satisfaz os ACs nunca foi commitado nem teve review formal de auth além do "YOLO" da 20.1. Recomendação: commitar o Epic 20 e tratar a re-review de auth como obrigatória (não dispensável). Severidade: LOW (processo). Fonte: Acceptance Auditor, review 20.2.

## Deferred from: code review of story-19.1 (2026-06-15)

- **Divergência de resolução de tema: inline-script (`layout.tsx`) × `ThemeProvider`/`getStoredTheme`.** Para um usuário novo (sem `theme` no `localStorage`) cujo SO está em modo claro: o script inline aplica `dark` por padrão e **não** consulta `prefers-color-scheme`, enquanto `getStoredTheme()` retornaria `light`. Resultado: o app pinta `dark` e o `useTheme()` JS passa a discordar do tema efetivamente pintado. **Não afeta o BrandLogo** (a troca é 100% CSS, segue a classe `.dark`/`.light` do `<html>`) e **não foi introduzido pela Story 19.1** — é comportamento do theme system (o default-dark pode ser intencional). Follow-up sugerido: decidir se o default deve honrar `prefers-color-scheme` no script inline, ou documentar o default-dark como intencional. Severidade: MEDIUM (UX), sem impacto funcional no logo. Fonte: Blind Hunter + Edge Case Hunter, review 19.1.

## Deferred from: code review of 21-6-janela-de-oportunidade-cross-campanha (2026-07-13)

- **TOCTOU cron+backfill concorrentes → card de engajamento duplicado cross-campanha.** [engagement-processor.ts:169-200](../../src/lib/utils/engagement-processor.ts#L169) — o dedup "ativo do mesmo lead" é um SELECT-then-INSERT não-atômico. Se o backfill admin roda sobreposto ao cron (≤5min) para o MESMO lead qualificando em 2 campanhas diferentes, ambos passam pelo dedup e inserem → 2 cards ativos. O índice parcial `uq_opportunities_engagement` é keyed em `(tenant, campaign, lead)` → não cobre cross-campanha. Janela estreita; self-corrige quando um card muda de status. Severidade: LOW. Fonte: Blind Hunter + Edge Case Hunter, review 21.6.
- **Upgrade engagement→reply não reseta `status` para `new`.** [reply-processor.ts:224-234](../../src/lib/utils/reply-processor.ts#L224) — um card já `viewed`/`contacted` promovido a reply (sinal mais forte) permanece no status atual em vez de re-emergir como `new`. Quando a Story 21.7 (notificações) definir o gatilho, um reply que promoveu um card já visto pode não disparar "nova oportunidade". Revisitar junto da 21.7. Severidade: LOW. Fonte: Blind Hunter, review 21.6.
- **`lt_interest_status` gravado sempre null para engagement (ramo `typeof number` morto).** [engagement-processor.ts:186-188](../../src/lib/utils/engagement-processor.ts#L186) — `LeadTracking.ltInterestStatus` é tipado `string`, então `typeof rawStatus === "number"` nunca é verdadeiro e a coluna fica sempre null. É conforme a spec (normalização string→int é 21.3), mas o sinal bruto de interesse é descartado nos rows de engagement. Carregar para 21.3/21.4 não assumirem a coluna preenchida. Severidade: LOW. Fonte: Acceptance Auditor, review 21.6.
- **Métricas de engajamento são write-once (open/click/last_engagement congelam na criação).** [engagement-processor.ts:167-184](../../src/lib/utils/engagement-processor.ts#L167) — o dedup "pula se já existe card ativo" nunca ATUALIZA o card existente, então as 3 métricas ficam no snapshot da 1ª qualificação; o card sub-reporta o engajamento contínuo até um reply promovê-lo. **Decisão Fabossi 2026-07-13: aceitar o snapshot; o refresh do card ativo é decidido/implementado na Story 21.4** (quando o card de engajamento é de fato renderizado — a 21.6 entrega só o modelo de dados). Severidade: LOW. Fonte: Blind Hunter, review 21.6.
- **Custo do piggyback cresce com o volume acumulado.** [engagement-processor.ts:247-295](../../src/lib/utils/engagement-processor.ts#L247) — a cada ciclo (≤5min) o processador re-varre todas as campanhas exportadas (`getLeadTracking` paginado até 50 págs) e re-executa `matchLeadId` + dedup SELECT para CADA lead qualificado, inclusive os já-carded. O custo (Instantly reqs + DB round-trips) escala com o total acumulado de leads qualificados, não com os novos. `fail-open` + idempotência cobrem 429/timeout na escala atual (~7 campanhas, poucas respostas), mas é um teto de escala. Otimização futura: pré-fetch em lote dos leads já-carded do tenant (um SELECT) e pular antes do `matchLeadId`, ou marcar leads já processados. Severidade: LOW (MEDIUM se o volume crescer). Fonte: Blind Hunter + Edge Case Hunter, review 21.6.

## Deferred from: code review of story-21.3 (2026-07-14)

- **Rotas do loop de resposta sem `maxDuration`** [`src/app/api/replies/process-batch/route.ts`, `src/app/api/replies/backfill/route.ts`] — o passe `classifyPendingReplies` é encadeado DEPOIS de sweep+process+engagement na mesma request. No pior caso (200 itens × até 15s de timeout / concorrência 10 ≈ 20 lotes ≈ 300s), a function pode ser morta no meio do passe, deixando classificação parcial e custo de OpenAI desperdiçado. Risco de infra pré-existente (as rotas já rodavam tarefas longas na 21.2/21.6) amplificado por esta story. Fix: `export const maxDuration` explícito nas duas rotas e/ou reduzir `MAX_CLASSIFY_PER_RUN`. Severidade: LOW. Fonte: Blind Hunter, review 21.3.
- **`updateLeadStatus` — read-modify-write em `leads.status` sem filtro `tenant_id`** [`src/lib/utils/reply-classifier.ts:361-400`] — o update de status do lead faz SELECT + UPDATE separados (RMW) por `.eq("id", leadId)` no cliente service-role (bypassa RLS). Duas oportunidades do MESMO lead no mesmo lote de concorrência (ou passes sobrepostos) podem interleave → lost update / status final não-determinístico (ex.: `opt_out` vs `interessado`). A guarda promote-only reduz o dano. `tenant_id` ausente é defesa-em-profundidade (leadId é PK único → funcionalmente correto). Inerente ao padrão `import-results` reusado. Fix: `.eq("tenant_id", tenantId)` no SELECT/UPDATE e/ou update condicional atômico. Severidade: LOW. Fonte: Blind Hunter + Edge Case Hunter, review 21.3.
- **Liveness — oportunidades "irresolvíveis" reentram para sempre + cap 200 global (não por-tenant)** [`src/lib/utils/reply-classifier.ts:477-523`] — fail-open `intent: null` não persiste, então rows que nunca resolvem (falha persistente de OpenAI, JSON truncado, ou sem-texto + `lt_interest_status ≤ 0`/null) são re-selecionadas em toda execução (`.is("intent", null)`, `order created_at asc`, `limit 200`). ≥200 dessas acumuladas travam a janela mais antiga e bloqueiam permanentemente as classificáveis seguintes; em cron (sem `tenantId`) o cap 200 é global → um tenant com backlog >200 monopoliza o passe. **Deferido por decisão do Fabossi (2026-07-14):** migration nova em `opportunities` num banco gerido à mão é risco maior que o bug na escala single-tenant atual (respostas de polling quase sempre têm `body.text` → caminho irresolvível raro). Fix quando escalar: coluna `classification_attempts` + backoff (para de re-selecionar após N falhas) **+** paginação/fairness por-tenant no cron. Gatilho natural: story 21.4 (Central expõe o backlog). Severidade: MEDIUM (LOW no curto prazo). Fonte: Blind Hunter + Edge Case Hunter, review 21.3.

## Deferred from: code review of 21-4-central-de-oportunidades-pagina-e-cards (2026-07-14)

- **PATCH aceita qualquer transição de status válida, incluindo regressões.** [src/app/api/opportunities/[opportunityId]/route.ts:41-61](../../src/app/api/opportunities/[opportunityId]/route.ts#L41) — a validação é só `isValidOpportunityStatus`, sem state-machine. Um card `discarded`/`meeting_booked` pode ser forçado de volta a `new`/`viewed` via PATCH direto, ressuscitando-o no badge `new` da sidebar e reordenando-o. Por design desta story o PATCH só suporta `new→viewed`; a guarda de transições + efeitos colaterais (`meeting_booked_at`, status do lead) são da Story 21.5 — a guarda de state-machine deve entrar junto. Severidade: LOW. Fonte: Blind Hunter, review 21.4.
- **Página pode ficar encalhada além de `totalPages` após marcar cards viewed sob filtro `status=new`.** [src/components/opportunities/OpportunitiesPageContent.tsx:150-198](../../src/components/opportunities/OpportunitiesPageContent.tsx#L150) — o paginador é gated por `totalPages > 1` e o `page` não reseta na mutation de mark-viewed (só em troca de filtro/perPage/clear). Ao marcar os cards da última página como viewed sob filtro `status=new`, o total encolhe, o refetch da página N atual volta um range vazio → empty-state; se `totalPages` cair a ≤1 o bloco inteiro de paginação (incl. "anterior") some, deixando o usuário preso numa página vazia sem controle de retorno. Fix: clamp de `page` a `totalPages` pós-refetch. Severidade: LOW. Fonte: Edge Case Hunter, review 21.4.
- **Invalidação da lista inteira a cada abertura de card (sem update otimista).** [src/hooks/use-opportunities.ts:220](../../src/hooks/use-opportunities.ts#L220) — abrir qualquer card `new` dispara PATCH e o `onSuccess` invalida `["opportunities"]` (refetch da página inteira) + `["opportunities-new-count"]`. Numa página de 25 cards, abrir vários casualmente gera um refetch de rede por abertura. É a transição passiva `new→viewed` — um update otimista/pontual do cache seria o correto; a invalidação total espelha o precedente do Insights (aceitável) mas é churn autoinfligido. Severidade: LOW. Fonte: Blind Hunter, review 21.4.

## Deferred from: dev-story 13.11 (fix envio WhatsApp do Insight — `campaign_id` nullable) (2026-07-15)

> ## 🟢 ATUALIZAÇÃO 2026-07-15 — 2 dos 3 itens abaixo JÁ FORAM RESOLVIDOS. Leia antes de pegar qualquer um.
>
> A **code review da 13.11 trouxe o read path para dentro do escopo dela** (decisão do Fabossi), e a **Task 5 foi executada**: a `00059` está aplicada no banco do cliente e validada end-to-end. Estado real de cada item:
>
> | Item | Dono original | Situação AGORA |
> |---|---|---|
> | 1. Fallback dangling da 21.5 ainda bloqueia o envio | 21.5 | 🔴 **ABERTO — segue com a 21.5.** Mas o pré-requisito venceu: a `00059` está aplicada, então a troca prevista no JSDoc está **destravada** e o fallback `NULL` já foi **provado em produção** pela validação da 13.11. |
> | 2. `campaigns!inner` esconde msg com `campaign_id NULL` | 21.5 | ✅ **RESOLVIDO pela code review da 13.11** — LEFT join + null-safe em [`route.ts:51`](../../src/app/api/leads/whatsapp-messages/route.ts#L51) + 9 testes novos de rota. **Provado contra o banco real**: a mensagem aparece no histórico sob "Sem campanha". **Não reabrir.** |
> | 3. Tipo `WhatsAppMessage.campaign_id: string` impreciso | 21.5 | ✅ **RESOLVIDO pela code review da 13.11** — agora `string \| null`. **Não reabrir.** |
>
> Por que os itens 2 e 3 mudaram de dono: as 3 camadas da review elegeram o `!inner` como achado nº1 e o Fabossi decidiu corrigir na 13.11 — o escopo da 21.5 é *oportunidades*, ela já estava em `review` (podia fechar sem pegar) e o fluxo afetado era 13.7/11.7. O item 3 veio junto porque, como o próprio registro abaixo antecipou, "a mudança pertence a quem tratar o `!inner`".
>
> **Novo item p/ a review da 21.5** (não estava aqui): `sendWhatsAppFromOpportunity` [`whatsapp.ts:~487`] loga `console.error(..., insertError)` com o **objeto inteiro** — o `details` do PostgREST traz `Failing row contains (...)` = telefone + corpo da mensagem (PII) no log. A 13.11 corrigiu o mesmo padrão nas 2 actions dela (logar só `code` + `message`) e deixou esta de fora por disciplina de escopo — é código da 21.5.

> Achados durante a implementação da 13.11 (migration `00059` = `DROP NOT NULL` em `whatsapp_messages.campaign_id`). **Nenhum é regressão e nenhum bloqueia a `00059`** — aplicar a migration é seguro: ela não reescreve linha alguma (mudança só de catálogo), nenhuma linha existente vira `null`, e afrouxar constraint não invalida dado que já passava na versão rígida. Os dois primeiros itens são consequências de segunda ordem que a `00059` torna *alcançáveis pela primeira vez*: hoje NENHUMA linha tem `campaign_id IS NULL` (o insert da 13.7 sempre falhava com `23502`). **Ambos pertencem à Story 21.5** (em `review`) e deveriam ser feitos no MESMO movimento — mexer neles a partir da 13.11 exigiria reescrever código e teste de uma story fora do escopo desta.

- **Caminho de campanha *dangling* da 21.5 ainda bloqueia o envio (troca prevista, não executada).** [src/actions/whatsapp.ts:436-452](../../src/actions/whatsapp.ts#L436-L452) — a 21.5 foi implementada ANTES da 13.11 (inversão da sequência planejada) e, seguindo a contingência que a própria story documenta ("Pré-requisito: Story 13.11"), o caminho de campanha deletada é **falha explícita** em vez do fallback `campaign_id: null` + envio: sem a `00059` a coluna era NOT NULL e não havia `null` a gravar. Com a `00059` aplicada, a troca prevista no JSDoc da action passa a ser possível — enquanto não for feita, **uma oportunidade cuja campanha foi deletada não envia WhatsApp** (falha com mensagem clara, não silenciosa; a Z-API não é chamada), que é exatamente o "bloquear um lead quente por causa de uma campanha deletada" que a decisão #3 do Fabossi queria evitar. **Importante: a `00059` NÃO muda o comportamento da 21.5 sozinha** — o bloqueio está no pré-check (código), não no schema; o caminho normal (campanha existe) grava o `campaign_id` real e sempre funcionou, com ou sem a 13.11. Fix: trocar a falha pelo fallback `null` + envio E virar o teste `campanha dangling → falha explícita SEM chamar a Z-API (00059 não aplicada)` [__tests__/unit/actions/whatsapp-from-opportunity.test.ts:300](../../__tests__/unit/actions/whatsapp-from-opportunity.test.ts#L300) para `→ insert com campaign_id: null e o envio ACONTECE`. **Dono: Story 21.5.** Severidade: MEDIUM (edge estreito, falha visível e não-silenciosa). Fonte: dev-story 13.11 (reconciliação 13.11 ↔ 21.5 pedida pelo Fabossi).

- **Mensagem com `campaign_id NULL` fica INVISÍVEL no histórico de WhatsApp do lead (`campaigns!inner`).** [src/app/api/leads/whatsapp-messages/route.ts:50](../../src/app/api/leads/whatsapp-messages/route.ts#L50) — a rota faz `.select("*, campaigns!inner(name)")`; o `!inner` é INNER JOIN, então uma linha com `campaign_id IS NULL` não casa com campanha alguma e é **descartada em silêncio**. Consequência: depois da `00059`, o envio a partir do Insight (13.7) grava e envia corretamente, mas a mensagem **não aparece** no histórico do lead — e o mesmo vale para o futuro fallback `null` do caminho dangling da 21.5 (item acima). Não é regressão (hoje essas linhas nem chegam a existir) e não bloqueia a `00059`; é a mesma família do bug original — código do Epic 11 assumindo que toda mensagem WhatsApp tem campanha. Os leitores POR CAMPANHA não são afetados: filtram `.eq("campaign_id", campaignId)`, e `null` nunca casa com UUID → linhas de insight ficam fora das métricas de campanha, que é o comportamento desejado ([tracking](../../src/app/api/campaigns/[campaignId]/leads/tracking/route.ts#L121), [whatsapp-messages](../../src/app/api/campaigns/[campaignId]/whatsapp-messages/route.ts#L64)). Fix: LEFT join (`campaigns(name)` sem `!inner`) + `campaign_name` degradando p/ null (a UI precisa de um rótulo tipo "Sem campanha"/"Envio a partir de insight"). **Dono: Story 21.5** (junto com o fallback — mesma causa, mesmo movimento). Severidade: MEDIUM (dado correto no banco, invisível na UI). Fonte: dev-story 13.11.

- **Tipo `WhatsAppMessage.campaign_id` declara `string`, mas a coluna passa a ser nullable.** [src/types/database.ts:77](../../src/types/database.ts#L77) — o tipo promete `campaign_id: string` enquanto o schema (pós-`00059`) permite NULL; o correto é `string | null`. Não quebra em runtime e não foi pego pelo `tsc` porque as actions do fluxo de insight/oportunidade fazem `as WhatsAppMessage` na volta do insert, silenciando a divergência. Deixado fora da 13.11 por escopo (a story é cirúrgica: 1 migration + log + testes) e porque `string | null` propaga para os consumidores — a mudança pertence a quem tratar o `!inner` acima, que é justamente quem vai precisar lidar com o null na UI. **Dono: Story 21.5.** Severidade: LOW (imprecisão de tipo; o cast já mascara). Fonte: dev-story 13.11.

## Deferred from: code review of 13-11-fix-envio-whatsapp-do-insight-campaign-id-nullable (2026-07-15)

> A review adversarial de 3 camadas **confirmou os 3 itens da seção acima** (dev-story 13.11) de forma independente — as três camadas, sem contexto prévio, elegeram o `campaigns!inner` como achado nº1. O registro do dev está factualmente correto; o que a review acrescenta é sobre **encaminhamento**, não sobre os fatos. Registrado aqui apenas o item deferido pela review; os dois `decision-needed` estão no story file.

- **`WhatsAppMessage.campaign_id: string` — por que o `tsc` nunca vai pegar isto (refina o item acima).** [src/types/database.ts:77](../../src/types/database.ts#L77) — deferido, pré-existente, mesmo item já registrado pelo dev-story. **Achado novo da review:** a divergência não é mascarada só pelos `as WhatsAppMessage` — `createServerClient` é chamado **sem o generic `Database`** [src/lib/supabase/server.ts:7](../../src/lib/supabase/server.ts#L7) (`grep -rn "createClient<" src/` → zero hits), então a interface `Database` [src/types/database.ts:197] **não vincula nada**: é decorativa, não protege consumidor algum e agora desinforma. Consequência para quem pegar este item: trocar para `string | null` é necessário mas **não suficiente** — enquanto o client não for tipado, nada garante que o tipo e o schema fiquem em sincronia. Isto é a mesma família do ponto cego sistêmico já registrado (mock não valida schema). Severidade: LOW. Fonte: Blind Hunter + Edge Case Hunter, code review 13.11.

## Deferred from: code review of 21-5-acoes-do-card-proximo-passo-por-ia (2026-07-15)

> Review adversarial de 3 camadas sobre a 21.5. Os 3 itens que a 13.11 deixou com "Dono: Story 21.5" foram reconciliados: **dois já tinham sido fechados pela própria 13.11** (`campaigns!inner` e `WhatsAppMessage.campaign_id`), e o terceiro (fallback dangling) foi confirmado pelas 3 camadas e virou **patch** desta review — sai desta lista. O item novo de PII que o dev-story da 13.11 antecipou para cá também virou patch. O que resta abaixo é o que é **pré-existente e sistêmico**, não causado pela 21.5.
>
> **Padrão nos 7 itens:** cinco deles (custo, TOCTOU, teto de KB, completion vazia, cache sem lock) são o mesmo ponto cego já registrado neste arquivo — *a suíte mocka o Supabase e a OpenAI, então nem constraint, nem concorrência, nem consumo real de token aparecem no verde*. Nenhum é regressão da 21.5; todos foram herdados do molde do Epic 13, que a story mandou espelhar.

- **Custo de IA hardcoded na tarifa do gpt-4o-mini enquanto o `model_preference` do tenant é honrado.** [src/lib/utils/opportunity-suggestion.ts:214](../../src/lib/utils/opportunity-suggestion.ts#L214) — `calculateSuggestionCost` (importado de `approach-suggestion.ts:50-55`) calcula `(prompt*0.15 + completion*0.6)/1M`, mas `loadNextStepPromptTemplate` respeita qualquer `model_preference` do prompt do tenant e o `callOpenAI` envia esse modelo. Se o tenant sobrescrever o prompt `opportunity_next_step` para `gpt-4o`, uma chamada de 800-in/200-out custa US$ 0,0040 e é registrada como US$ 0,00024 — **~16x sub-reportado**. O teste da própria story exercita `model_preference: "gpt-4o"` e assere `body.model === "gpt-4o"`, provando que o caminho diverge e não é coberto p/ custo. **Pré-existente e idêntico** em `approach-suggestion.ts:214` (Epic 13) — é do molde, não da 21.5. Relevância elevada: o custo é a base de precificação do cliente (memória do projeto: "custos não podem errar"). Fix: derivar a tarifa do modelo efetivamente usado, ou travar o modelo quando a tarifa é fixa. Severidade: MEDIUM. Fonte: Blind Hunter, code review 21.5.

- **Guarda de state-machine do PATCH é read-then-write (TOCTOU).** [src/app/api/opportunities/[opportunityId]/route.ts:86-137](../../src/app/api/opportunities/[opportunityId]/route.ts#L86-L137) — o SELECT de `current.status` e o UPDATE são statements separados, e o UPDATE filtra só `.eq("id")` + `.eq("tenant_id")`, sem predicado de status. A guarda é **consultiva sob concorrência**: dois PATCHes que leem `viewed` passam ambos pelo `isRegression`, e o card pode terminar `discarded` **com `meeting_booked_at` preenchido** (envenena o ROI do Epic 23). O idioma atômico está no mesmo patch — `whatsapp.ts:533` faz `.in("status", ["new","viewed"])` no próprio UPDATE. Não é regressão (a 21.4 não tinha guarda alguma) e o fix "correto" não é trivial: aplicar o predicado no UPDATE faz 0 linhas → `PGRST116` → 404, quando o certo seria 409 — precisa distinguir "não existe" de "transição perdeu a corrida". Mesma família do TOCTOU cron+backfill já deferido na 21.6. Single-tenant, baixa concorrência real. Severidade: MEDIUM. Fonte: Blind Hunter + Edge Case Hunter, code review 21.5.

- **A guarda de custo trunca o único input limitado e deixa os ilimitados crus.** [src/lib/utils/opportunity-suggestion.ts:51](../../src/lib/utils/opportunity-suggestion.ts#L51) — `MAX_REPLY_TEXT_CHARS = 4000` corta **só** o `replyText`; `company_context`, `products_services`, `competitive_advantages` e `icp_summary` são interpolados direto do `loadKBContext` sem teto, e `maxTokens: 500` limita **apenas a saída**. A decisão #1 do Fabossi (gerar automático ao abrir) foi autorizada sobre uma premissa de "prompt ~2.000 tokens" que a própria story marca como frágil (*"se a KB do tenant for muito extensa, o prompt cresce"*). Um tenant com 40k chars de KB manda ~12k tokens por abertura de card — uma ordem de grandeza acima do modelado, escalando com a KB e limitado por nada. A resposta do lead, o único campo com teto, costuma ser o menor contribuinte. Pré-existente no molde do Epic 13. Severidade: MEDIUM. Fonte: Blind Hunter, code review 21.5.

- **Completion vazia com tokens consumidos: cobra, loga como sucesso, não cacheia e re-tenta sem cap.** [src/app/api/opportunities/[opportunityId]/suggestion/route.ts:152-167](../../src/app/api/opportunities/[opportunityId]/suggestion/route.ts#L152-L167) — o gerador devolve `{suggestion: null, promptTokens: 800, completionTokens: 200}` no ramo "Rascunho vazio retornado", ou seja **com tokens preservados**. A rota então: (1) loga custo real com `status: "success"` — uma geração que não produziu nada fica classificada como sucesso no exato registro que o AC6 existe para auditar; (2) `if (!suggestion) return` **sem gravar cache**; e o card [OpportunityCard.tsx:140] reseta `draftRequestedRef = false` sob o comentário *"Erro não gera custo"* — falso justamente neste ramo. Resultado: um prompt que trave um filtro de conteúdo gera uma chamada paga a **cada clique no card** (e o bloco de geração não é gated em `expanded`, então **fechar** o card também dispara), sem cap e sem sinal na UI. A única guarda contra custo desgovernado (cache no servidor, decisão #1) é contornada pelo ramo que consumiu token. O `error` que o gerador retorna é descartado no destructure da rota — nada registra o porquê. Fix ambíguo de propósito (cachear sentinela? não resetar o ref? tratar vazio como erro-duro?) → decisão de produto. Severidade: MEDIUM. Fonte: Blind Hunter + Edge Case Hunter + Acceptance Auditor, code review 21.5.

- **`opportunityId` não-UUID devolve 500 em vez de 404/400.** [src/app/api/opportunities/[opportunityId]/suggestion/route.ts:89](../../src/app/api/opportunities/[opportunityId]/suggestion/route.ts#L89) — nenhuma das duas rotas valida o path param antes do `.eq("id", opportunityId)` contra coluna UUID (o server action valida, via `z.string().uuid()`). O Postgres levanta `22P02 invalid input syntax for type uuid`, cujo code **não** é `PGRST116`, então cai no ramo genérico `INTERNAL_ERROR`. URL velha/colada errado vira 500 + ruído de erro, e o 500 mascara falha real de infra no mesmo ramo. Pré-existente: o PATCH da 21.4 já tinha o padrão. Severidade: LOW. Fonte: Edge Case Hunter, code review 21.5.

- **Cache do rascunho é read-then-write sem lock — 2 abas geram e pagam 2x.** [src/app/api/opportunities/[opportunityId]/suggestion/route.ts:103-174](../../src/app/api/opportunities/[opportunityId]/suggestion/route.ts#L103-L174) — `if (row.suggestion && !regenerate) return cache` … `await generateOpportunityNextStep(...)` … `.update({ suggestion })`: sem lock, sem write condicional (`.is("suggestion", null)`), sem dedup de in-flight. Fora do servidor, a única barreira é o `draftRequestedRef` (estado de componente). Duas abas na mesma oportunidade → ambas leem `null`, ambas chamam a OpenAI, ambas logam custo, a segunda sobrescreve a primeira. A decisão #1 ("1 geração por oportunidade para sempre") vale para o caso sequencial. Custo real do pior caso: uma chamada extra. Severidade: LOW. Fonte: Blind Hunter, code review 21.5.

- **Rejeição do update pós-`sendText` marca `failed` numa mensagem JÁ ENVIADA → lead recebe 2x.** [src/actions/whatsapp.ts:508-571](../../src/actions/whatsapp.ts#L508-L571) — o `try` envolve o `zapi.sendText` **e** o update `status:"sent"` seguinte. O código trata `updateError` com elegância (retorna `success:true` com a linha sintetizada), mas uma **rejeição** desse mesmo update (queda de rede/Supabase) cai no catch, que marca a linha `failed` e devolve "Falha ao enviar mensagem WhatsApp". Nenhuma flag distingue "envio ainda não tentado" de "envio já bem-sucedido". Consequência: a Z-API entregou a mensagem, o operador vê toast vermelho, o composer não fecha (`success=false`), ele clica Enviar de novo — **o lead recebe o mesmo WhatsApp duas vezes** e a oportunidade nunca é auto-marcada `contacted`. Mesma forma em `sendWhatsAppFromInsight` (molde) → pré-existente. Severidade: LOW (exige rejeição, não erro-objeto, na janela entre `sendText` e o update). Fonte: Edge Case Hunter, code review 21.5.
