# ==============================================
# TDEC Prospect - Environment Variables Template
# ==============================================
# Copy this file to .env.local and fill in your values
# cp .env.example .env.local

# ----------------------------------------------
# Supabase Configuration (Required)
# ----------------------------------------------
# Get these from: https://supabase.com/dashboard/project/_/settings/api

# [NEXT_PUBLIC_ = client-side] Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# [NEXT_PUBLIC_ = client-side] Supabase anonymous key (safe for browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# [server-only] Supabase service role key (NEVER expose to client)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# ----------------------------------------------
# App Configuration (Required for deploy)
# ----------------------------------------------

# [NEXT_PUBLIC_ = client-side] Site URL for redirects (login, invites, password reset)
# In production (Vercel): set to your deployed URL (e.g., https://tdec-prospect.vercel.app)
# In dev: not required (fallback to localhost:3000 or window.location.origin)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ----------------------------------------------
# Security Configuration (Required)
# ----------------------------------------------

# [server-only] Encryption key for API keys stored in database
# Generate with: openssl rand -hex 32
API_KEYS_ENCRYPTION_KEY=your_32_byte_hex_key_here

# ----------------------------------------------
# AI Providers (Optional - Required for AI features)
# ----------------------------------------------

# [server-only] OpenAI API key (fallback — app preferentially uses key stored in database)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_key

# ----------------------------------------------
# External Services (Optional - Required for specific features)
# ----------------------------------------------
# NOTE: Most API keys (Apollo, SignalHire, Instantly, Snov.io, Apify)
# are stored ENCRYPTED in the database via Settings > Integrations.
# The env vars below are only needed for services that require
# server-side configuration beyond the stored API key.

# [server-only] SignalHire callback URL for phone lookup results
# Format: https://<project-ref>.supabase.co/functions/v1/signalhire-callback
# Deploy with: npx supabase functions deploy signalhire-callback --no-verify-jwt
SIGNALHIRE_CALLBACK_URL=https://your-project.supabase.co/functions/v1/signalhire-callback

# ----------------------------------------------
# Z-API / WhatsApp (Epic 11)
# ----------------------------------------------
# NOTE: Z-API credentials (Instance ID, Instance Token, Security Token)
# are stored in the database via Settings > Integrations, NOT as env vars.
# No environment variables needed for WhatsApp integration.

# ----------------------------------------------
# Test Environment Only (DO NOT set in production)
# ----------------------------------------------
# TEST_USER_EMAIL=test@example.com
# TEST_USER_PASSWORD=test_password
